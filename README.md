# ГЭУ Диалог

Дипломный проект: веб-приложение для приемной комиссии СПбГЭУ. Система работает как чат-бот для абитуриентов, умеет проверять позиции абитуриента в конкурсных списках, сохранять проверки сотрудников и анализировать ошибки ответов агента по базе источников.

Публичная версия: [https://geu-dialog.onrender.com](https://geu-dialog.onrender.com)

## Что умеет проект

- Отвечает на типовые вопросы абитуриентов.
- Распознает код абитуриента и ищет его в подключенных конкурсных списках.
- Загружает конкурсные списки через серверный proxy, чтобы обойти ограничения браузера и CORS.
- Показывает найденные позиции абитуриента по конкурсным группам.
- Сохраняет историю диалогов и журнал проверок сотрудника.
- Позволяет вручную проверить ответ чат-бота через форму `Тест анализа`.
- Выполняет поиск релевантных источников по правильному ответу сотрудника.
- Сравнивает ответ бота, источник бота и правильный ответ сотрудника.
- Использует LLM для связного объяснения причины ошибки.
- Выводит доказательства в виде текстовых фрагментов, а не только названий файлов и страниц.

## Основные сценарии

### 1. Абитуриент спрашивает у бота

Пользователь открывает сайт, задает вопрос или вводит код абитуриента. Если введен код, приложение ищет его в конкурсных списках. Если введен обычный вопрос, бот отвечает по локальной базе знаний.

### 2. Сотрудник проверяет ответ

Сотрудник может отметить ответ как правильный или неправильный, добавить корректный ответ и сохранить проверку в журнал.

### 3. Ручной тест анализа ошибки

В панели `Тест анализа` вручную вводятся:

1. вопрос абитуриента;
2. ответ чат-бота;
3. фрагмент источника, на который опирался чат-бот;
4. правильный ответ сотрудника.

После этого агент:

1. ищет top-k источники по правильному ответу сотрудника;
2. сравнивает найденные источники с источником ответа бота;
3. определяет тип ошибки;
4. вызывает LLM для нормального текстового объяснения;
5. показывает причину, рекомендацию, top-k фрагменты и доказательства.

## Архитектура

```text
Браузер пользователя
        |
        | index.html / style.css / script.js
        v
Node.js server.js
        |
        |-- static files
        |-- SQLite: data/dialogs.sqlite
        |-- proxy к priem.unecon.ru
        |-- /api/source-search
        |-- /api/error-analysis
        v
error_analysis_agent.js
        |
        |-- data/combined_data.json
        |-- semantic embeddings + cosine similarity
        |-- local rules
        |-- LLM: pollinations / openai-fast
```

## Структура файлов

```text
.
├── index.html                         # HTML интерфейса
├── style.css                          # стили интерфейса
├── script.js                          # логика фронтенда, чат, парсер, UI проверок
├── server.js                          # HTTP сервер, API, proxy, SQLite
├── error_analysis_agent.js            # агент анализа ошибок: поиск, правила, LLM
├── run_error_analysis_tests.js         # тесты агента анализа ошибок
├── package.json                       # npm scripts и версия Node.js
├── render.yaml                        # конфигурация Render
├── DEPLOY_RENDER.md                   # инструкция деплоя
├── ERROR_ANALYSIS_README.md           # отдельное описание агента анализа
├── data/
│   ├── combined_data.json             # база источников для поиска
│   ├── error_analysis_test_cases.json # тестовые кейсы
│   ├── error_analysis_report.json     # отчет локального теста
│   ├── error_analysis_report_llm.json # отчет теста с LLM
│   └── dialogs.sqlite                 # локальная SQLite база истории
└── spbgeu-icon.*                      # иконки приложения
```

Главный файл анализа ошибок: `error_analysis_agent.js`.

## Создание интерфейса

Интерфейс проекта сделан без React/Vue и без сборщика. Это обычное одностраничное приложение на `HTML`, `CSS` и `JavaScript`.

Основные файлы интерфейса:

```text
index.html  # структура страницы
style.css   # внешний вид, адаптивность, панели, кнопки, сообщения
script.js   # логика чата, режимов, парсинга, проверок и обращения к API
```

Такой вариант выбран потому, что проект должен легко запускаться локально и на Render без сложной сборки.

### Общая структура экрана

Интерфейс состоит из нескольких основных зон:

```text
┌──────────────────────────────────────────────┐
│ Левая панель                                 │
│ - название проекта                           │
│ - кнопки режимов                             │
│ - список диалогов                            │
│ - журнал проверок                            │
├──────────────────────────────────────────────┤
│ Центральная область чата                     │
│ - верхняя панель активного диалога           │
│ - сообщения                                  │
│ - поле ввода                                 │
└──────────────────────────────────────────────┘

Дополнительно:
┌──────────────────────────────────────────────┐
│ Правая выдвижная панель проверок             │
│ - журнал оценок сотрудника                   │
│ - форма "Тест анализа ошибки"                │
│ - вывод результата анализа                   │
└──────────────────────────────────────────────┘
```

### Этапы создания интерфейса

1. Создан базовый HTML-каркас страницы в `index.html`.
2. Добавлена боковая панель с названием проекта, режимами и списком диалогов.
3. Добавлена центральная область чата: заголовок, контейнер сообщений и поле ввода.
4. Добавлены сообщения пользователя и бота с разным визуальным оформлением.
5. Добавлен режим сотрудника приемной комиссии.
6. Добавлена возможность оценивать ответы бота как правильные или неправильные.
7. Добавлен журнал проверок сотрудника.
8. Добавлена правая панель с фильтрами проверок.
9. Добавлена форма ручного теста анализа ошибки.
10. Добавлена прокрутка результата анализа, чтобы длинные объяснения и доказательства не обрезались.
11. Добавлен вывод top-k источников в виде текстовых фрагментов.
12. Добавлен вывод LLM-анализа: причина, объяснение, рекомендация и доказательства.

### Боковая панель

Боковая панель нужна для навигации по приложению.

В ней находятся:

- название приложения;
- кнопка создания нового диалога;
- переключение режима абитуриента и сотрудника;
- список сохраненных диалогов;
- кнопка открытия журнала проверок;
- кнопка открытия ручного теста анализа ошибки.

Логика находится в `script.js`:

- создание нового чата;
- переключение активного чата;
- отрисовка списка диалогов;
- сохранение состояния в SQLite через API;
- восстановление состояния при загрузке страницы.

### Центральная область чата

Центральная область - основной рабочий экран пользователя.

Она содержит:

- заголовок активного диалога;
- бейдж режима;
- список сообщений;
- поле ввода;
- кнопку отправки;
- подсказку для пользователя.

Когда пользователь отправляет сообщение:

1. текст добавляется в чат как сообщение пользователя;
2. запускается обработчик `handleUserQuery`;
3. если найден код абитуриента, включается поиск по конкурсным спискам;
4. если это обычный вопрос, выбирается ответ из локальной базы знаний;
5. ответ бота добавляется в чат;
6. сообщение сохраняется через API.

### Режим абитуриента

Режим абитуриента предназначен для обычного пользователя.

В этом режиме можно:

- задавать вопросы;
- вводить код абитуриента;
- получать позиции в конкурсных списках;
- видеть источники, по которым был сформирован ответ.

Пример запроса:

```text
проверь код 4393384
```

Если код найден, бот показывает:

- конкурсную группу;
- форму обучения;
- основу обучения;
- раздел списка;
- позицию абитуриента.

### Режим сотрудника

Режим сотрудника нужен для проверки качества ответов.

Сотрудник может:

- отметить ответ как правильный;
- отметить ответ как неправильный;
- добавить правильный ответ;
- сохранить исправление;
- открыть журнал проверок;
- посмотреть анализ ошибки.

Эти данные сохраняются в таблицу `reviews` в SQLite.

### Журнал проверок

Журнал проверок отображается в правой панели.

В журнале видно:

- вопрос пользователя;
- ответ бота;
- оценку сотрудника;
- правильный ответ;
- источник ответа;
- анализ ошибки;
- дату проверки.

Есть фильтры:

- все проверки;
- неправильные;
- правильные.

### Ручной тест анализа ошибки

Форма `Тест анализа ошибки` была добавлена для проверки агента без полного диалога с ботом.

Поля формы:

```text
Вопрос абитуриента
Ответ чат-бота
Фрагмент источника, на который опирался чат-бот
Правильный ответ сотрудника
```

Важно: в поле источника нужно вставлять именно текстовый фрагмент, а не только название файла или ссылку. Если текст вставлен, агент считает, что источник есть, даже если у него нет файла или страницы.

После нажатия `Проанализировать` фронтенд отправляет запрос:

```http
POST /api/error-analysis
```

Тело запроса:

```json
{
  "question": "есть ли военная кафедра",
  "agentAnswer": "военная кафедра предоставляется лишь студентам аспирантам",
  "agentSources": [
    {
      "title": "фрагмент",
      "text": "военная кафедра предоставляется всем студентам очной формы обучения"
    }
  ],
  "adminAnswer": "военная кафедра предоставляется всем студентам очной формы обучения",
  "topK": 5
}
```

На экране выводится:

- тип ошибки;
- связное объяснение;
- рекомендация;
- использованная LLM;
- количество текстовых источников в базе;
- top-k найденных фрагментов;
- доказательства.

### Вывод результата анализа

Результат анализа специально выводится не только как техническая метка, а как связное объяснение.

Пример:

```text
Ответ противоречит переданному источнику.

Агент ответил, что военная кафедра предоставляется лишь студентам аспирантам,
однако в переданном фрагменте указано, что она предоставляется всем студентам
очной формы обучения. Эталонный ответ сотрудника совпадает с этим фрагментом.
Таким образом, агент добавил недопустимое ограничение, которое не содержится
в источнике.
```

Такой вывод нужен, чтобы сотрудник или преподаватель видел не просто `wrong-source` или `needs-review`, а понимал конкретную причину ошибки.

### Стили интерфейса

Стили находятся в `style.css`.

Основные задачи CSS:

- сделать интерфейс похожим на рабочий инструмент приемной комиссии;
- визуально разделить сообщения пользователя и бота;
- выделить режимы работы;
- сделать удобный список диалогов;
- оформить правую панель проверок;
- добавить прокрутку для длинных результатов анализа;
- сохранить читаемость на разных размерах экрана.

Ключевые блоки стилей:

```text
sidebar                 # левая панель
chat-container          # центральная область чата
messages-container      # список сообщений
message                 # сообщение пользователя или бота
review-drawer           # правая панель проверок
manual-analysis-panel   # ручной тест анализа
manual-analysis-result  # результат анализа ошибки
```

### JavaScript-логика интерфейса

Основная логика находится в `script.js`.

Крупные части файла:

- состояние приложения: чаты, сообщения, режим, проверки;
- локальная база знаний для простых ответов;
- поиск абитуриента в конкурсных списках;
- разбор HTML-таблиц конкурсных списков;
- сохранение данных через API;
- отрисовка сообщений;
- отрисовка журнала проверок;
- отправка ручного анализа на `/api/error-analysis`;
- вывод результата анализа на экран.

### Связь интерфейса с сервером

Фронтенд не обращается напрямую к файлам или внешним сайтам. Для этого используется `server.js`.

Примеры запросов:

```text
GET  /api/health
GET  /api/state
POST /api/chats
POST /api/reviews
GET  /api/admission-list?url=...
POST /api/source-search
POST /api/error-analysis
```

Функция `apiRequest` в `script.js` отвечает за отправку запросов и обработку ошибок.

### Почему интерфейс сделан отдельной страницей, а не Telegram-ботом

В дипломном прототипе веб-интерфейс удобнее, потому что:

- можно показать чат и проверку ответов в одном месте;
- сотрудник видит журнал проверок;
- можно вручную вводить тестовые кейсы;
- проще демонстрировать работу парсера конкурсных списков;
- проще развернуть на Render и открыть с любого компьютера.

### Что важно показать на защите

1. Открыть публичный сайт.
2. Задать обычный вопрос абитуриента.
3. Проверить код абитуриента.
4. Открыть журнал проверок.
5. Открыть `Тест анализа`.
6. Ввести вопрос, ответ бота, фрагмент источника и правильный ответ.
7. Показать, что агент находит top-k фрагменты.
8. Показать, что LLM дает связное объяснение ошибки.
9. Показать, что в доказательствах выводятся текстовые фрагменты.

## Требования

- Node.js `>= 22.19.0`
- Браузер
- Для полной работы анализа нужен файл `data/combined_data.json`
- Для LLM-слоя на Render используется `pollinations / openai-fast`

Проект не требует сборки фронтенда: интерфейс написан на обычных `HTML`, `CSS`, `JavaScript`.

## Локальный запуск

Перейти в папку проекта:

```powershell
cd C:\Users\Acer\Desktop\Диплом
```

Запустить сервер:

```powershell
node --no-warnings server.js
```

Или через npm:

```powershell
npm start
```

После запуска открыть:

```text
http://localhost:3000
```

Проверка сервера:

```text
http://localhost:3000/api/health
```

Ожидаемый ответ:

```json
{
  "ok": true,
  "app": "ГЭУ Диалог"
}
```

## Переменные окружения

### Основные

| Переменная | Назначение | Значение по умолчанию |
|---|---|---|
| `PORT` | порт сервера | `3000` локально, на Render задается платформой |
| `HOST` | адрес прослушивания | `0.0.0.0` |
| `SOURCE_DATA_PATH` | путь к `combined_data.json`, если файл лежит не в `data/` | пусто |

### LLM

На Render используется:

```text
LLM_PROVIDER=pollinations
LLM_MODEL=openai-fast
LLM_TIMEOUT_MS=120000
LLM_MAX_TOKENS=1000
```

Для OpenAI-compatible API можно задать:

```powershell
$env:LLM_ENDPOINT = "https://api.openai.com/v1/chat/completions"
$env:LLM_API_KEY = "ваш_api_ключ"
$env:LLM_MODEL = "gpt-4o-mini"
```

Если LLM недоступна, агент использует локальный fallback: поиск и правила анализа остаются рабочими, но объяснение будет менее гибким.

## API

### `GET /api/health`

Проверяет, что сервер работает.

### `GET /api/state`

Возвращает сохраненные диалоги, сообщения и проверки из SQLite.

### `POST /api/state/import`

Импортирует состояние из старого локального хранилища браузера в SQLite.

### `POST /api/chats`

Создает или обновляет чат.

### `POST /api/chats/:id/messages`

Сохраняет сообщения конкретного чата.

### `DELETE /api/chats/:id`

Удаляет чат, его сообщения и связанные проверки.

### `GET /api/reviews`

Возвращает журнал проверок сотрудника.

### `POST /api/reviews`

Сохраняет проверку ответа бота.

### `GET /api/admission-list?url=...`

Серверный proxy для загрузки конкурсного списка с `priem.unecon.ru`.

Нужен потому, что браузер не всегда может напрямую загрузить таблицы с внешнего сайта из-за CORS и сетевых ограничений.

### `POST /api/source-search`

Ищет top-k источники по тексту.

Пример:

```json
{
  "query": "военная кафедра предоставляется всем студентам очной формы обучения",
  "topK": 5
}
```

Ответ содержит найденные фрагменты:

```json
{
  "sources": [
    {
      "id": "source-367",
      "score": 0.2558,
      "fileName": "general_faq.json",
      "pageNumber": "3",
      "preview": "### Есть ли военная кафедра? Нет, но есть программа военной подготовки..."
    }
  ],
  "stats": {
    "textSources": 723
  }
}
```

### `POST /api/error-analysis`

Запускает полный анализ ошибки.

Пример запроса:

```json
{
  "question": "есть ли военная кафедра",
  "agentAnswer": "военная кафедра предоставляется лишь студентам аспирантам",
  "agentSources": [
    {
      "title": "фрагмент",
      "text": "военная кафедра предоставляется всем студентам очной формы обучения"
    }
  ],
  "adminAnswer": "военная кафедра предоставляется всем студентам очной формы обучения",
  "topK": 5
}
```

Пример результата:

```json
{
  "analysis": {
    "reasonType": "nonexistent-direction",
    "reasonTitle": "Ответ противоречит переданному источнику",
    "reasonText": "Агент ответил, что военная кафедра предоставляется лишь студентам аспирантам, однако в переданном фрагменте указано, что она предоставляется всем студентам очной формы обучения...",
    "recommendation": "При формировании ответа проверять, чтобы итоговая формулировка не вводила новые ограничения...",
    "suggestedSource": "Переданный фрагмент источника агента",
    "topSources": [],
    "evidence": []
  },
  "stats": {
    "textSources": 723
  }
}
```

## Как работает парсер конкурсных списков

Логика находится в `script.js` и `server.js`.

1. Пользователь вводит код абитуриента.
2. Фронтенд распознает числовой код.
3. Приложение получает список конкурсных групп.
4. Для каждой конкурсной группы формируется URL на `priem.unecon.ru`.
5. HTML конкурсного списка загружается через `/api/admission-list`.
6. Фронтенд разбирает таблицы в HTML.
7. В строках таблицы ищется код абитуриента.
8. Если код найден, система определяет конкурсную группу, форму обучения, основу, раздел списка и позицию.
9. Ответ форматируется для пользователя.

Для ускорения используется ограничение параллельности: списки проверяются не все сразу, а пачками.

Если загрузка внешнего сайта не удалась, бот сообщает, что конкурсные списки не удалось получить, и предлагает проверить запуск `server.js`.

## Как работает агент анализа ошибок

Главный файл: `error_analysis_agent.js`.

На вход подается:

1. `question` - вопрос абитуриента;
2. `agentAnswer` - ответ бота;
3. `agentSources` - фрагмент источника, на который опирался бот;
4. `adminAnswer` - правильный ответ сотрудника.

Дальше выполняются шаги:

1. Загружается `data/combined_data.json`.
2. Из базы исключаются записи с `"label": "TABLE"`.
3. Текстовые источники переводятся в embedding-векторы.
4. По `adminAnswer` выполняется семантический поиск top-k источников.
5. Одинаковые найденные фрагменты удаляются из top-k.
6. Найденные источники сравниваются с `agentSources`.
7. Ответ бота сравнивается с правильным ответом сотрудника.
8. Локальные правила определяют базовый тип ошибки.
9. LLM получает вопрос, ответ бота, источник бота, правильный ответ, top-k и базовый анализ.
10. LLM формирует связное объяснение ошибки.
11. Backend нормализует результат, чтобы в доказательствах были текстовые фрагменты, а не только файл или страница.

### Типы ошибок

| Тип | Значение |
|---|---|
| `wrong-source` | неправильно выбран источник |
| `merged-rows` | объединены несколько строк или фрагментов |
| `nonexistent-direction` | появились лишние строки, направления или приписки |
| `incomplete-answer` | ответ неполный |
| `missing-agent-source` | источник агента не сохранен |
| `missing-admin-answer` | нет правильного ответа сотрудника |
| `needs-review` | нужна ручная проверка |

### Пример связного объяснения LLM

Если бот ответил:

```text
военная кафедра предоставляется лишь студентам аспирантам
```

а переданный источник был:

```text
военная кафедра предоставляется всем студентам очной формы обучения
```

агент должен объяснить:

```text
Агент ответил, что военная кафедра предоставляется лишь студентам аспирантам, однако в переданном фрагменте указано, что она предоставляется всем студентам очной формы обучения. Эталонный ответ сотрудника совпадает с этим фрагментом. Таким образом, агент добавил недопустимое ограничение, которое не содержится в источнике.
```

## Векторный поиск

В проекте используется семантический поиск по embedding-индексу источников. Индекс строится локально в файле `data/source_embedding_index.json`, поэтому отдельная внешняя векторная база для прототипа не требуется.

Алгоритм:

1. загружается `data/combined_data.json`;
2. записи с `"label": "TABLE"` исключаются;
3. каждый текстовый фрагмент переводится в эмбеддинг моделью `Xenova/paraphrase-multilingual-MiniLM-L12-v2`;
4. эмбеддинги сохраняются в `data/source_embedding_index.json`;
5. правильный ответ сотрудника переводится в эмбеддинг той же моделью;
6. запрос сравнивается с векторами источников через cosine similarity;
7. выбираются top-k ближайших фрагментов;
8. найденные источники передаются в модуль анализа ошибки и LLM-слой.

Если embedding-модель недоступна, приложение использует резервный TF-IDF-поиск. Это нужно только для устойчивости: основной сценарий рассчитан на семантические эмбеддинги.

Это значит, что другой пользователь не обязан иметь свою базу или локальный поиск. На публичном сайте все выполняется на Render:

```text
браузер пользователя -> Render -> server.js -> combined_data.json -> результат
```

## База данных

Для истории диалогов и журнала проверок используется SQLite:

```text
data/dialogs.sqlite
```

Таблицы:

- `chats` - список диалогов;
- `messages` - сообщения внутри диалогов;
- `reviews` - проверки ответов сотрудником.

Важно: на бесплатном Render SQLite может сбрасываться при перезапуске сервиса, потому что файловая система не предназначена для постоянного хранения. Для дипломной демонстрации это допустимо. Для продакшена лучше заменить SQLite на Postgres.

## Тестирование

Запуск локальных тестов анализа:

```powershell
node run_error_analysis_tests.js
```

Запуск тестов с LLM:

```powershell
node run_error_analysis_tests.js --llm
```

Тестовые кейсы:

```text
data/error_analysis_test_cases.json
```

Отчеты:

```text
data/error_analysis_report.json
data/error_analysis_report_llm.json
```

Последняя локальная проверка агента:

```text
Тестов: 18
Совпало с ожидаемой меткой: 18/18
Текстовых источников в базе: 723
```

## Деплой на Render

Файл конфигурации:

```text
render.yaml
```

Основные настройки:

```yaml
services:
  - type: web
    name: geu-dialog
    env: node
    plan: free
    buildCommand: ""
    startCommand: "node --no-warnings server.js"
    healthCheckPath: /api/health
```

После деплоя приложение доступно по ссылке:

```text
https://geu-dialog.onrender.com
```

На бесплатном тарифе Render сервис может засыпать. Первый запрос после паузы может выполняться дольше обычного.

## Частые проблемы

### Сайт открывается, но конкурсные списки не загружаются

Проверить:

- запущен ли `server.js`;
- работает ли `/api/health`;
- доступен ли `priem.unecon.ru`;
- не изменилась ли структура HTML таблиц на сайте приемной комиссии.

### На локальном компьютере код абитуриента находится, а у другого пользователя нет

Если пользователь открывает `localhost`, он обращается к своему компьютеру, а не к вашему. Для внешнего доступа нужно открывать публичную ссылку Render:

```text
https://geu-dialog.onrender.com
```

### Cloudflare Tunnel Error 1033

Это означает, что туннель `cloudflared` не работает или компьютер выключен. Решение - использовать деплой на Render, тогда сайт не зависит от включенного компьютера.

### Агент пишет fallback вместо LLM

Причины:

- LLM-сервис временно не ответил;
- не заданы переменные окружения;
- истек timeout;
- внешний сервис вернул ошибку.

При этом локальный анализ все равно выполняется.

### `combined_data.json` не найден

Файл должен лежать здесь:

```text
data/combined_data.json
```

Или нужно задать:

```powershell
$env:SOURCE_DATA_PATH = "путь_к_combined_data.json"
```

## Что отправлять преподавателю

Если нужно отправить именно код анализа:

```text
error_analysis_agent.js
server.js
run_error_analysis_tests.js
data/error_analysis_test_cases.json
data/error_analysis_report.json
```

Если нужно отправить проект целиком:

```text
index.html
style.css
script.js
server.js
error_analysis_agent.js
package.json
render.yaml
data/combined_data.json
data/error_analysis_test_cases.json
```

Главная логика анализа находится в:

```text
error_analysis_agent.js
```

## Ограничения проекта

- Качество поиска зависит от качества и полноты `combined_data.json`.
- LLM может формулировать ответы немного по-разному.
- Структура сайта `priem.unecon.ru` может измениться, тогда парсер таблиц придется адаптировать.
- Бесплатный Render может засыпать.
- SQLite на Render не гарантирует постоянное хранение данных.

## Краткое резюме

Проект объединяет чат-бота приемной комиссии, парсер конкурсных списков и агента анализа ошибок. Важная часть диплома - не только ответ бота, но и проверка качества этого ответа: система сравнивает ответ агента с источником и эталонным ответом сотрудника, ищет релевантные фрагменты в базе источников и с помощью LLM формирует понятное объяснение ошибки.
